n2=4000;
D1=zeros(n2,2);
X2=zeros(n2,1);
noise=-1+2*rand(n2,1);  % generate uniform noise on the interval [-1 1]
D1(:,1)=X2+.0001*noise;   % add noise to the data
Y2=-1+2*rand(n2,1);
D1(:,2)=Y2;
n2=4000;
D2=zeros(n2,2);
X2=(-1+2*rand(n2,1))/2;
noise=-1+2*rand(n2,1);  % generate uniform noise on the interval [-1 1]
D2(:,1)=X2+.0001*noise;   % add noise to the data
D2(:,2)=-X2*sqrt(3);
n3=4000;
D3=zeros(n3,2);
X3=-1/2+rand(n3,1);
noise=-1+2*rand(n3,1);  % generate uniform noise on the interval [-1 1]
D3(:,1)=X3;
Y3=X3/sqrt(3);
D3(:,2)=Y3+.0001*noise;
D=[D1;D2;D3];
[A,idx]=main_PBC3(D,0.036,0.024,0.006,5,3);